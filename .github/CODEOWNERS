# Default owners for everything not matched below.
* @Synxpsis @tinkerfasttrack

# The design system is the source of truth for UI rules across the whole
# client. Changes here affect every screen, so route them past the owner.
/design-system/ @Synxpsis
/CLAUDE.md @Synxpsis

# CI/deploy pipeline and branch rules — changes here affect what "safe to
# merge" means for everyone, not just the PR author.
/.github/ @Synxpsis
