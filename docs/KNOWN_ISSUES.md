# Known issues

A ledger, not a wishlist — things that are actually broken, missing, or inconsistent right now, with
where they were found and their current status. Feature ideas and roadmap belong wherever the team
tracks those, not here. Update this file in the same commit that fixes or newly discovers something.

## Open bugs

### `game.html`'s `Escape` key never clears a selection
Two `case 'Escape':` arms exist in the same `switch` inside the keyboard handler
(`_onKey` around `game.html:440-444`) — `case 'Escape': this.exit(); break;` and, a few lines later,
another `case 'Escape':` that clears `multi`/`marquee`/`sel`/`armed`/`mode`. JavaScript `switch`
matches the first case with that value; the second arm is dead code. Escape always exits to the hub;
it can never just clear a selection. Found during the Command Console migration
(`design-system/HANDOFF-GAPS.md`, "Not design-system related" section), left untouched at the time as
out of scope for that pass. **Still open.**

### Production's live `duel` board has zero cities right now
`GET /api/maps/duel` against production returned `"cities": []` as of 2026-09-14 — no capitals, no
income, for either seat, in every real match currently playing on it (AI or 1v1). Found while
investigating a report that an AI match's board looked different from the editor's — it wasn't; both
correctly read the same live, broken board (see the "Fixed this session" entry below for the
read/write-are-fine finding, and the capital-flag fix this prompted). Not caused by this session's own
changes — this is the live GM's own in-progress edit, mid-repositioning a capital when reported. The new
save-time validation (`handlePutMap`, `FPMap.capitalIssues()`) stops this from being *saved* into again
going forward, but doesn't retroactively fix data already in D1 before it existed. **Still open** — needs
a GM to open `editor.supremacy.live`, use each sector's "Make capital of this sector" toggle (Capitals
panel shows exactly which sectors still need one), and Save.

## Design-system gaps

The Command Console v1.0 migration surfaced a longer list of token gaps, judgment calls, and
deliberate scope decisions during its rollout — full detail in
[`design-system/HANDOFF-GAPS.md`](../design-system/HANDOFF-GAPS.md) rather than duplicated here. Short
version of what's still open there:

- **Tokens v1.1** (2026-09-19) resolved two of these: `--sl-seam-signal` was gold when Foundations
  drew it white — fixed, and every raw `rgba(255,255,255,0.55)` hover-border literal in the codebase
  now reads that token instead. `--sl-action-free` (aliased to `--sl-info`) gives Reinforce's button
  colour an actual name; `game.html` reads it now instead of `--sl-info` directly.
- No translucent-ink token — **partially resolved**: v1.1 adds `--sl-veil-000/100/200`, but nothing
  consuming this shape has been migrated to them yet. Canvas label plates and the match-HUD hint bar
  still solve it independently (a hand-rolled hex→rgba cache for canvas, `color-mix()` for DOM).
- The hub's 1v1-queue "live queue" state has no armed-frame treatment, despite Foundations naming it
  as an armed-frame example.
- `--sl-focus-ring` isn't retrofitted onto every interactive element (nothing violates the
  accessibility floor — no bare `outline: none` — but the polished custom ring isn't wired up broadly).
- **`game.html`'s match sidebar animates its own `width` open/closed (2026-09-14, mechanism changed
  2026-09-15)** — a deliberate, user-approved exception to non-negotiable #8 (in-match motion capped at
  220ms, colour and opacity only): a layout-geometry property, not colour/opacity. Duration still
  respects `--sl-dur-3` (220ms) and gets `prefers-reduced-motion` handling for free via the existing
  token, so the *only* actual rule broken is the property-type restriction, not the timing one. See the
  "Fixed this session (2026-09-14)" table below for the full tradeoff this was weighed against (a
  fixed-width, always-on rail, which would have needed no exception at all). Originally animated
  `grid-template-columns` on the whole match layout, which resized the board canvas's own layout box
  (and therefore its WebGL backing store) every frame of the transition — reported as the camera
  flickering while the sidebar moved. The sidebar is now an absolutely-positioned overlay
  (`var(--sl-z-hud)`) on top of a board layer that's always full-size (`var(--sl-z-board)`), so only the
  sidebar's own box animates; the board's is untouched by this transition at all. See the
  fixed-this-session table below for that fix's own row and commit.

## Fixed this session (2026-09-11 – 2026-09-13)

Kept here for traceability — these were real bugs, now resolved on `main`:

| Issue | Fix | Commit |
|---|---|---|
| Assigning a sector's starting seat in `editor.html`'s new selector (added earlier this session) didn't colour the sector — only a city placed exactly on the sector's centre tile, with a matching `seat`, appeared to "set the whole sector's colour." Reported live within minutes of that feature shipping. | `overlay()` built `owners` purely from individual cities' own `seat` fields, which `sim.create()` never actually reads for ownership — only `seedOwners()` (from `starts`) does, and `territoryOwner()` only ever checks a sector's centre tile, so a city's `seat` could only ever reach that one check by sitting exactly there. `overlay()` now seeds `owners` from `FPMap.seedOwners(S.built)` first — the same call `sim.create()` makes at kickoff — so a sector's whole footprint colours the instant a seat is assigned, matching a real match. Also capped starting-seat assignment at one owned sector per seat (assigning a new one moves the seat's home rather than creating a second). See [MAP_SYSTEM.md](MAP_SYSTEM.md), [EDITOR_UPGRADE.md](EDITOR_UPGRADE.md). | `f92fefb` |
| `editor.html` occasionally threw "Cannot read properties of undefined (reading 'create')" in the header on first login, self-resolving on a later repaint | Race between `board-render-3d.js`'s ES-module load (fetches `three`/`OrbitControls` from a CDN) and `boot()`'s same-origin auth/map fetches — worst for a returning user with `'3d'` saved as their render-mode preference. `ensure3D()` guarded the `S.built`-not-ready race but not `self.FPRender3D` not being set yet. Now waits for a `fp-render-3d-ready` event instead of assuming, mirroring the guard `game.html`'s boot polling already had for the same race. Found during design-system commit review, not something the pre-placed-structures work touched. | `2269007` |
| In `editor.html`'s 3D view, deleting or adding a city could leave a stale (or missing) piece rendered — it kept showing after a delete, or didn't appear after an add — even though the side panel and click hit-testing already reflected the correct, current board. Reported as "deleting a city seems to be confused with deleting a territory," which was the visible symptom, not the actual cause. | `board-render-3d.js`'s city/capital piece loop read `M.cities` — the board object handed to `create()` once at scene setup — instead of fresh data each `update()` call, the way ownership/garrisons/barracks/industry already worked. Editing a city doesn't otherwise need `reset3D()`'s full scene rebuild (only land/water layout is baked into a fixed-instance mesh; individual pieces are freely added/removed), so this only ever surfaced as a stale render, not a data bug. `update()` now accepts the live list as `o.cities`; `editor.html` passes `S.built.cities` every frame, `game.html` doesn't (a match's cities never change after kickoff) and keeps its original behaviour untouched. See [RENDERING.md](RENDERING.md). | `19bcb4a` |
| In `editor.html`'s 3D view, the game's camera-lock-on-selection (recentre + freeze rotate/pan) engaged on almost every click, since every click there selects *something* (even open water) — unlike `game.html`, where it only fires on an armed march order. Fighting the lock made it easy to end up clicking a neighbouring tile (e.g. landing on "Remove territory" when aiming for a city's "Clear tile"). | `create()` gained a `selectionLock` option (default true, preserving `game.html`'s behaviour unchanged); `editor.html` passes `false`, so its 3D view always orbits freely regardless of selection. Also added a two-click "Clear board" action (cities + structures, keeps territories) so starting a board from scratch doesn't require deleting tile-by-tile. See [EDITOR_UPGRADE.md](EDITOR_UPGRADE.md). | `19bcb4a` |
| 3D troop-count labels froze on screen at their last position after a march completed, ignoring all further camera movement | A `CSS2DObject` nested under a removed parent (`sphere.remove(label)` was missing before `scene.remove(sphere)`) never got its DOM node cleaned up — Three only fires the removal event on the object removed directly, not recursively. See [RENDERING.md](RENDERING.md). | `e6101e5` |
| 3D garrison/troop/march labels rendered dead-centered on their tile/piece/sphere instead of offset above it | `CSS2DRenderer` overwrites `element.style.transform` inline every frame from the object's `.center`, silently overriding the CSS `transform` rule each label declared for its offset. Switched to `CSS2DObject.center`. | `e6101e5` |
| 3D camera had no selection-centering or lock — free to pan/rotate away from a tile mid-order | Selecting/arming a tile now recentres the orbit target and freezes rotate/pan (zoom stays live) until the selection clears. | `e6101e5` |
| `build()`/`industry()` allowed constructing on a tile owned individually even when the surrounding territory's capital hadn't fallen yet — territory was still contested | Both commands now also require `territoryOwner(M, S.owners, p) === seat`, returning `'territory not secured'` otherwise. | `dec3489` |
| A seat reduced to zero territories saw no game-over and had no legal moves left, since the win check only fired once the *other* seat reached the map's needed count | Added an elimination check in `step()`: zero territories = immediate loss, gated to real 2-seat matches so `solo` (no real seat 1) isn't affected. | `dec3489` |
| AI test match defaulted to the `grand` board (25 territories) instead of the board ranked 1v1 actually uses | Hub's test-match link and `game.html`'s board-resolution fallback both now default to `duel`; `grand` remains fully defined and reachable via explicit `?board=grand`. | `a74602b` |
| 3D board tiles were a flat, paper-thin plane — hard to visually distinguish from the void background, no sense of raised ground | Land tiles are now an extruded slab (top face still at `y=0`, so no overlay needed changes); gap tiles get a recessed water mesh instead of being hidden. See [RENDERING.md](RENDERING.md). | `628eda2` |
| In the 3D renderer, every structure (capitals, cities, barracks, industry) was coloured via a hardcoded seat 0 = self / seat 1 = foe mapping instead of the viewer-relative one ground tiles already used correctly — a seat-1 player saw their own capital in the opponent's colour while the ground under it was correctly self-coloured. Found via live 1v1 testing between two real accounts. | `upsertPiece()` now takes the same viewer-relative `seatColour` closure `update()` already threads through to every ground tile, instead of reaching for the standalone hardcoded function. Verified by rebuilding a live match's 3D scene under a forced seat-1 perspective. | `931a8c2` |
| The production `duel` board's live D1 content had lost its **entire `cities` array** (not just blank names, as first suspected — the array was completely empty: `"cities": []`). No capitals existed anywhere, so capital income (`sim.js`'s `cap.wealth` payout, which requires an actual city object at the centre tile) never fired for anyone — capturing territory produced no economic benefit at all, which is what "taking territory doesn't do anything" was actually describing. Ownership/combat itself was unaffected (that logic is purely tile-coordinate-based, no city object needed). | Restored via `PUT /api/maps/duel` with `map.js`'s known-good static `MAPS.duel` definition (real names, all 8 cities, correct wealth/garrison/seat values) — confirmed both by the automatic symmetry check passing and by re-reading `/api/maps/duel` afterward. A pre-existing corruption, not something this session's own editor changes caused (last write predates this session by ~2 days per its `updatedAt`). | data-only, no commit — see `worker.js`'s `handlePutMap` |
| The 3D camera's one-time initial placement (`game.html`'s `wire()`) offset from the player's own home by a **fixed world-space direction**, not one mirrored per seat the way the 2D camera's `viewYaw()` correctly is. Home sits in a different board corner per seat, so the fixed offset only pointed toward the board's interior for seat 0; for seat 1 it pointed *away* from the board entirely, past their own home's outer corner, into a steep near-horizontal grazing angle across the whole map. Live 1v1 testing reported this as two symptoms of the one bug: the camera "started backwards," and the board "looked black" (a raking angle lights mostly tile side-walls, not tops — read as missing colour, but wasn't). Distinct from the structure-piece colour bug above, found in the same testing pass — fixing one didn't fix the other. | Camera now offsets along the vector from home toward the board's centre — the same direction on every board, automatically mirrored for whichever corner a seat's home is in — instead of a fixed absolute direction. Verified seat 0's framing is pixel-identical (the math reduces to the old fixed offset for a home in that corner) and that a forced seat-1 rebuild now gets the same well-lit, correctly-oriented framing, with no black board. | `282c5d7` |

## Fixed this session (2026-09-14)

| Issue | Fix | Commit |
|---|---|---|
| In `editor.html`'s 3D view, removing a sector left its land tiles, internal grid lines, and territory border permanently stuck on screen — even though the tile inspector correctly called the same spot open water. Reported as "sector still shows the tile grid" after `remove sector` was clicked and confirmed. | `removeTerritory()` called `select(null)` (which repaints) *between* `reset3D()` and `rebuild()`. Since the 3D scene only rebuilds lazily on the next paint, that stray repaint let `ensure3D()` eagerly recreate the scene from the still-stale board, and `rebuild()`'s own repaint right after found the scene already existed and skipped rebuilding it. Reordered to `reset3D()` → `rebuild()` → `select(null)`, matching `addTerritory()`'s already-correct order. See [EDITOR_UPGRADE.md](EDITOR_UPGRADE.md), [MAP_SYSTEM.md](MAP_SYSTEM.md). | `855e2bf` |
| An empty (water) slot in the 3D view had no grid lines between its 49 individually clickable tiles, so it rendered as one undivided flat quad — visually indistinguishable from a single giant cell, despite each tile underneath still being independently inspectable. | The tile-grid line-drawing loop only ever iterated existing provinces. Now iterates every slot in the board, land or water alike, drawing water's lines at the water surface height instead of the land surface. See [RENDERING.md](RENDERING.md). | `855e2bf` |
| Any `editor.html` edit that changes land/water layout (add/remove sector, resize the slot grid or block size) forces a full 3D scene rebuild, which reset the camera to the default overview framing every time — so orbiting or zooming in, then making one small edit, snapped the view straight back out. Reported directly after the above fix shipped. | `reset3D()` now reads the outgoing scene's camera position and orbit target back out before disposing it; the next scene creation seeds the fresh camera/controls with that saved pose instead of the default framing, falling back to the default only when there's no prior pose (first load, board swap). See [RENDERING.md](RENDERING.md). | `45068e4` |
| The live-match sidebar (`game.html`) collapses to a 0px-wide grid column when nothing's selected, but a stray corner of its "Nothing selected" placeholder's dashed border still leaked through the grid gap into the bottom-left of the viewport. Reported as "a little white thing in the bottom left corner." | The `aside` had `min-width: 0` (correctly letting its own grid track collapse) but no `overflow: hidden`, so its inner content div — which never got its own `min-width: 0` and kept its min-content width — overflowed the 0px box instead of actually shrinking into it. `aside` now clips its own overflow, the same pattern already used one element over on the board wrapper div. | `4323b2e` |
| `game.html`'s 3D camera recentred onto whatever was selected and froze rotate/pan on *every* tile click (not just an armed march order) — fighting free navigation during ordinary play the same way `editor.html`'s own now-fixed version of this used to (see the 2026-09-13 entry above). Explicitly requested after the editor's camera fixes above: "I really like what we did with the editor camera, and I want to keep that." | `game.html` now passes `selectionLock: false` to `create()`, matching the editor. The march-drag gesture that the lock's original comment cited as the reason for it (`_onDown3D`/`_onUp3D`) already disables/re-enables `controls.enabled` directly around the drag itself, independent of `selectionLock`, so turning the broader lock off doesn't reopen that original concern — verified live: dragging a garrison to march it still holds the camera steady through the drag. See [RENDERING.md](RENDERING.md) → Camera. | `4323b2e` |
| An AI-opponent match's board never matched what was actually saved in the editor — renaming a sector, moving a capital, or any other edit only ever showed up in a real networked 1v1, never against the AI. Reported as "the map for AI battles is not the same map as what we built in the editor." | `game.html`'s local (non-networked) `play()` built its sim straight from the static bundled `self.FPMap.MAPS[key]`, unlike the networked path, which already reads the server's live D1-backed board (`match.js`'s `loadBoardDef()`) and sends the resolved definition down as `boardDef`. An AI match never talks to the Match Durable Object at all, so it had no equivalent. `play()` now awaits a new `loadBoardDef()` that fetches `GET /api/maps/:key` itself before building the sim, falling back to the static bundle only if that fetch fails — same fallback shape as the server's own version. Briefly shows the existing "connecting" screen while the fetch is in flight (previously instant, now a genuine round-trip). Verified: a sector renamed and saved in the editor showed up in a fresh AI match's `GET /api/maps/duel` response. | `a63effd` |
| The live-match sidebar and camera-aspect resize snapped open/closed instantly on every tile click/deselect — reported as wanting "a graceful come out, and put away" with the camera "stabilizing without snapping." | `grid-template-columns` on the match's outer grid now transitions (`var(--sl-dur-3)`/`var(--sl-ease-mono)`, 220ms) instead of snapping between `0px 1fr` and `348px 1fr`; the board canvas's existing `ResizeObserver` → `resize()` hookup already re-syncs the 3D camera's aspect ratio on every size change, so it now eases smoothly across the transition's frames for free, with no camera-specific code needed. **This is a deliberate, approved exception to CLAUDE.md's non-negotiable #8** ("in-match motion is capped at 220ms and limited to colour and opacity") — `grid-template-columns` is neither colour nor opacity. Flagged to the user before implementing; they explicitly chose the animated-width option over the fully rule-compliant alternative (reserving the sidebar's width at a constant 300px at all times, per CLAUDE.md's own in-match side-rail spec, so the canvas — and therefore the camera — would never need to resize on selection at all). See the Design-system gaps bullet below. | `a63effd` |
| A capital used to have to sit on its province's exact geometric centre tile (`capitalOf()`'s old position-match logic) — which the game's own shipped default `duel`/`solo` boards never actually satisfied (their cities are hand-placed off-centre by design), so capital income had been silently zero for every territory on those boards, editor bug or not. Compounded by a real editor gap: nothing stopped saving a sector with zero cities at all, which is what actually broke production (see the open-bugs entry above). | `capitalOf()` now finds a city by an explicit `capital: true` field, independent of position; the editor's city inspector gained a "Make capital of this sector" toggle, a live "Capitals" panel (checked on every board, not just `duel`), and `symmetry()` now checks the flag matches a city's twin. `worker.js`'s `handlePutMap` hard-refuses to save any board with a province that has zero or more than one capital, on any key. `duel`/`solo`'s static definitions and `grand`'s generated one all got the flag added to their existing (or, for `grand`, already-centred) capital city. See [EDITOR_UPGRADE.md](EDITOR_UPGRADE.md), [MAP_SYSTEM.md](MAP_SYSTEM.md), [MECHANICS.md](MECHANICS.md), [GLOSSARY.md](GLOSSARY.md). Also fixed a related display bug found in the same pass: `game.html`'s sidebar showed a selected city's `wealth` as "Coin / sec" for *any* city, capital or not — only the capital's wealth is ever actually paid. | `092833a` |
| Reported immediately after the above shipped: saving still failed with "sector missing a capital — sector01 has no capital..." even after marking capitals on the two named sectors, and the message named sectors the GM couldn't identify ("Not sure what Sector 1 is, or Sector 10"). Not actually a validation bug — those two sectors (blank-named, from an earlier session) genuinely had zero cities in them yet — but `capitalIssues()`'s fallback (`p.name \|\| p.id`) surfaced the raw internal id slug, meaningless to a GM. Also prompted a explicit ask: stop shipping `duel`/`solo`/`grand` with built-in fantasy names (Verrand, Dunmar, ...) at all — "default every sector to no name, and let me create a name for each one individually," applied to every board so the editor and every game mode stay in sync. | Added `describeProvince()`/`describeCity()` (map.js) — name if set, else a `(c, r)` centre-tile coordinate, the same format the tile inspector already uses for unnamed ground. `capitalIssues()` and every `symmetry()` issue message use these now instead of raw `.name`/`.id`. `duel`, `solo`, and `grand`'s static province definitions had their built-in names blanked (city names unchanged) — same blank-by-default a sector added through the editor already got. | `75c9685` |
| Reported as "the neutral capitals only read as 30" despite being authored at 75 — a genuinely wrong-data bug, not the perceived-void rendering issue below. `FPSim.create()`'s kickoff garrison seeding never read a capital city's own `garrison` field at all; it always used the flat `RULES.neutralCapital` (30) / `RULES.homeCapital` (20) constants, silently discarding whatever a GM actually authored. Pre-existing (not caused by the capital-decoupling work above), but only became visible once a GM had a real reason to set a specific neutral-capital garrison. | Neutral territories now read the capital's own `garrison` when set, falling back to the constant only if unset; seat-owned (home) territories are unaffected (a home capital has no `garrison` field by design, so `RULES.homeCapital` remains the only source). The seed position is unchanged — still the centre tile (the capture point), never the capital's own position — moving it would leave the actual capture point undefended. See [MECHANICS.md](MECHANICS.md) → Kickoff garrison. | `7264c0d` |
| Alongside the above, a screenshot of an early-game AI match appeared to show an unclaimed neutral territory as a "pentagon in a black void," read as more evidence of a data-sync bug. It wasn't — watched live, it was a correctly-synced neutral capital on real, correctly-drawn grid tiles that flipped to fully legible red the moment the AI captured it a few ticks later. The actual cause: 3D grid lines were drawn in `--sl-ink-000`, the *exact colour of the void background itself* — invisible by construction, not a data problem. | Grid lines (land and water both) now use the design system's `--sl-seam-strong`/`--sl-seam-dash` tones instead, at those tokens' own opacities. See [RENDERING.md](RENDERING.md) → Ground. | `7264c0d` |
| Reported as "red objects on red tiles" — a captured territory's capital piece hard to make out against its own ground in the editor's 3D view. A real, concrete inconsistency: `board-render.js`'s 2D `piece()` has always brightened a structure's roof colour above the raw faction colour (`shade(seatColour(own), 1.5)`) specifically so it reads as distinct from ground filled with a *dimmed* version of that same colour — `board-render-3d.js`'s `upsertPiece()` never got the equivalent treatment and used the raw, unbrightened colour, worst from a top-down-favouring angle where a piece's flat top face (lit like the ground beside it) outweighs its shaded side walls. | Added a `shade()` to `board-render-3d.js` — the identical algorithm 2D already uses, adapted for the numeric hex this module caches tokens as — and applied it to piece colour the same way 2D's `piece()` does. Verified live against production data at both a normal and a steep top-down angle. See [RENDERING.md](RENDERING.md) → Structures. | `c2b3e4d` |
| Reported: the camera/renderer flickers as the match sidebar animates open and closed, in both AI and networked matches. The sidebar's width transition was a `grid-template-columns` animation on the whole match layout — the board canvas shared that grid with the sidebar, so its own layout box (and therefore its WebGL backing store) resized on every frame of the transition, forcing a `renderer.setSize()`/new framebuffer each time, racing the browser's own repaint of the animating layout. | The outer layout is now a plain flex column (header, then a `position: relative` wrapper) instead of a grid. The board div is an absolutely-positioned base layer (`var(--sl-z-board)`) filling that wrapper unconditionally — its box never changes size when the sidebar opens or closes. The sidebar is an absolutely-positioned overlay (`var(--sl-z-hud)`) on top of it, still easing its own `width` between `0px` and `348px` on the same transition/duration as before — same animation, now fully decoupled from the board's layout. `gridCols` renamed to `asideWidth` to match. Verified locally: sidebar opens/closes over the board with no shift or resize, hit-testing and the march-drag gesture unaffected. | `5b9b998` |

## Fixed this session (2026-09-20)

| Issue | Fix | Commit |
|---|---|---|
| A sector's exposed 3D wall (its side faces, below the top surface) was tinted the owning seat's faction colour, or `--sl-ink-300` if unclaimed — the same `instanceColor` that colours its top face. Reported directly from reference screenshots: a live match's coastline should read as "island," the wall colour independent of who controls the sector, not a flat-coloured slab in the owner's hue. Separately, water tiles floated above the true bottom of a coastline — `WATER_DEPTH` (0.05) was far shallower than `LAND_DEPTH` (0.26), leaving a visible gap between water's underside and the sand wall's actual bottom. | Land is now two stacked `InstancedMesh`es instead of one: a thin, still per-instance-coloured "cap" over a deeper "wall" that never receives an `instanceColor` at all, so its material's flat `--sl-terrain-sand` colour (a new token — none of the achromatic/status palette read as "coastline") applies untouched. `WATER_DEPTH` is now derived (`LAND_DEPTH - WATER_SINK`) so water's bottom face lines up with the wall's bottom face exactly, no floating gap. See [RENDERING.md](RENDERING.md) → Ground. | `31addae` |
| In `editor.html`'s 3D view, a grid-growth "+" handle was a thin `0.05`-tall plate floating just above `y = 0` — nowhere near a real sector's total depth — so it read as a flat marker hovering over open space rather than the same solid block a placed sector would be. The gap tile's water mesh underneath it (a handle only ever sits on an empty/water slot) also rendered right through and around it. | `board-render-3d.js` now exports its `LAND_DEPTH` constant so the handle's box always matches a real sector's depth exactly, and a new `setWaterMask(cells)` on the module's returned API hides water under every tile a handle covers. See [RENDERING.md](RENDERING.md) → Ground, [EDITOR_UPGRADE.md](EDITOR_UPGRADE.md). | `31addae` |
| Shipped-then-immediately-broken: a grow-handle click used to place land immediately with no way to grow into water on purpose (fixed in `4016cce`, same session) — the fix made a plain "+" click grow the grid and open the "Open water" tile inspector (Add land sector / Leave as water) instead of placing land outright. Reported right after deploying: the fast, one-click "add a sector" action now took two easy-to-miss steps through a side panel, and clicking several different "+" handles (expecting the old behaviour) just grew a scatter of empty water slots instead of the sectors intended — "stuck," "spawns water sectors in different spots." | Reverted the plain-click path to `addTerritory()` (land, instant, exactly the pre-regression behaviour) and moved the water choice to **Shift+click**, which commits that slot as water outright — no inspector, no second step. `growSlotsOnly()` now grows the grid and dismisses the selection (`select(null)`) instead of opening it. See [EDITOR_UPGRADE.md](EDITOR_UPGRADE.md). | `cc1f8bd` |

## Known limitations (by design or by scope, not bugs)

- **No reconnect** in a live match — a dropped connection is a forfeit after a 15s grace period, with
  no way to rejoin. Explicit v1 scope decision (`src/match.js`'s header comment), not an oversight.
- **No spectate or replay.** The client always renders "seat 0" as the viewing player with no seat
  remapping — a structural blocker for either feature, not just a missing menu option.
- **2-seat matches only.** Nothing in the sim, matchmaker, or board model supports more than two
  players in one match.
- **`grand` board shelved** (see [OVERVIEW.md](OVERVIEW.md), [MAP_SYSTEM.md](MAP_SYSTEM.md)) — not
  deleted, just not defaulted to anywhere as of 2026-09-12.
- **Faction palette not user-selectable** despite four being fully defined — see
  [CONTENT_INVENTORY.md](CONTENT_INVENTORY.md).
- **`public/proto3d.html`** is unmigrated scratch code, not a shipped screen — see
  `design-system/HANDOFF-GAPS.md` item 9.
- **The profile screen (`public/profile.html` and `index.html`'s in-app `profile` screen) ships a
  deliberately reduced version of the `profile-and-lobby-swipe` design handoff / the design system's
  `Supremacy DS — Screens II.dc.html` "04 Profile" reference** — that concept's rating-history chart,
  hero stats (rating/ladder rank/trophies), rank title, badges panel and faction panel all assume
  systems that don't exist anywhere in this codebase (`src/match.js` never persists a match outcome
  to D1; there is no ELO/rating/rank field on `users` at all; badges and faction are unscoped).
  Building any of that for real today would mean fabricating the numbers it shows, which the design
  files themselves explicitly rule out ("rating band is real data, never a fake progress bar"). What
  actually shipped (2026-09-16, extended 2026-09-19): real account info (username, member-since,
  last-seen), plus the hero-stats/rating-graph/match-history *layout* with an honest "not tracked
  yet"/em-dash in place of numbers, and "coming soon" for badges/faction. Rank title was left out
  entirely rather than given a placeholder — there's no tier list yet to even reference. The
  lobby↔profile drag/swipe transition from that same handoff did ship for real (see
  [OVERVIEW.md](OVERVIEW.md)); it needed no new data, unlike the profile content itself. Scope
  choice, not an oversight — revisit the stats/graph/badges/faction/rank sections once their
  backing systems exist.
